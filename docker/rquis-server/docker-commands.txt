// attach to a running docker container and then add users, etc
sudo docker exec -i -t 665b4a1e17b6 /bin/bash // get the id by sudo docker ps

// add users
useradd user25
echo "user25:pass25" | chpasswd 
rm -rf /home/user25
mkdir  /home/user25
chown user25:user25 /home/user25
addgroup user25 staff

// list groups
getent group

//list users
getent passwd

//chnage password
passwd username

// groups:
groups
groups user1
usermod -a -G groupName userName // add to group


sudo docker build -t rquis-server .
sudo docker run rquis-server
sudo docker run -d -p 8787:8787 rquis-server
sudo docker run -i -t rquis-server /bin/bash
sudo docker rmi rquis-server


sudo docker build -t javadch/rquis/server:0.4.1 .
sudo docker run javadch/rquis/server:0.4.1
sudo docker run -d -p 8787:8787 javadch/rquis/server:0.4.1
sudo docker run -i -t javadch/rquis/server:0.4.1 /bin/bash
sudo docker rmi javadch/rquis/server:0.4.1

sudo docker images


docker info

docker ps


# removes all dangling images
sudo docker rmi $(sudo docker images -f "dangling=true" -q)
sudo docker rmi $(sudo docker images -q)

# removes all containers
sudo docker rm $(sudo docker ps -aq)

# stop containers
sudo docker stop $(sudo docker ps -aq)
sudo docker stop 7116620319

# save an image to locall disk
sudo docker save -o rqs_041.tar javadch/rquis/server:0.4.1

# load am image from local disk
docker load --input rqs_041.tar

# transfer an image to a remote machine


# save, transfer, load at once
sudo docker save javadch/rquis/server:0.4.1 | bzip2 | pv | ssh ubuntu@idiv-tmp.inf-bb.uni-jena.de 'bunzip2 | sudo docker load'

sudo docker save javadch/rquis/server:0.4.1 | sudo docker $(idiv-tmp.inf-bb.uni-jena.de) load


docker-machine scp /home/javadch/projects/phd/src/Docker/rquis/rqs_041.tar idiv-tmp.inf-bb.uni-jena.de:/home/ubuntu
docker-machine ssh idiv-tmp.inf-bb.uni-jena.de sudo docker load -i rqs_041.tar

# use github: push dockerfile and needed data to DEV, pull on target machine, build and run.

